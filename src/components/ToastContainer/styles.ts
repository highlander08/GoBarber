import styled from 'styled-components';

export const Container = styled.div`
  /**positionando o tosta no canto superiro direito */
  position: absolute;
  right: 0;
  top: 0;
  padding: 30px;
  overflow: hidden;
`;
